pipeline {
    agent any
    stages {
        stage('Install dependencies') {
            steps {
            	echo 'now installing dependencies ....'
                sh 'npm install --save'
            }
        }
    }
}

